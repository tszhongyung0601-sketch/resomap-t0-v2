# ResoMap V4 — AI 對話 × 旅行文件

兩個新東西，共用一條規則：**畫面上寫的每一件事，背後都要真的發生。**

品牌色、橘色 header、字體、手機外框、bottom nav 四個 tab、卡片樣式、圓角、間距、
既有圖片、探索頁、導覽庫全部不動。

---

## 0. 七個已拍板的決定

| # | 決定 |
|---|---|
| 1 | AI 對話**腳本式，不接模型** |
| 2 | 對話能**產新行程，也能改現有行程** |
| 3 | 文件**能真解析的就真解析** |
| 4 | 解析結果**要寫進行程前先問過使用者** |
| 5 | 條碼用 **zxing 套件**，所有瀏覽器都能讀 |
| 6 | 五步驟精靈**留著**，跟對話並存 |
| 7 | Demo **附一張偽造的範例登機證** |

---

## 1. 為什麼「腳本式」不是次級品

`lib/planner.ts` 的檔頭已經寫了這件事：這個 app 的「AI 排行程」不呼叫模型、沒有亂數，
同樣的輸入永遠給同樣的行程。對話沿用同一條紀律。

差別在**它不會假裝**。畫面上不會出現三個跳動的點假裝在思考，也不會說「我來想想」。
它做的是：把你打的字解析成一個意圖跟幾個參數，交給已經在跑的 planner，
然後把結果當成**提案**給你看。看不懂就直說看不懂，並列出它聽得懂的說法。

一個聽不懂還硬掰的對話框，比一個誠實說「這句我不會」的對話框糟得多——
前者會讓人失去對整個 app 的信任，後者只是這一句沒接上。

---

## 2. 對話引擎

### 2.1 檔案

```
lib/chat/intent.ts     文字 → { intent, slots }。純函式，沒有 React
lib/chat/entities.ts   抽地名、天數、興趣、景點名、第幾天
lib/chat/respond.ts    intent → 回覆 + 提案（Proposal）
lib/chat/intent.test.mjs   真實句子的測試，跑 node 就好
```

### 2.2 聽得懂什麼

| intent | 例句 | 動作 |
|---|---|---|
| `plan` | 花蓮三天兩夜、想去台南玩兩天、幫我排日月潭 | `generatePlan()` → 提案新行程 |
| `refine` | 多一點美食、不要走太多路、改成租車 | 帶著上次的參數重跑 |
| `addStop` | 第二天加七星潭、想去東大門夜市 | 提案加一站 |
| `removeStop` | 把松園別館拿掉、第一天不要去太魯閣 | 提案刪一站 |
| `moveStop` | 七星潭改到第三天 | 提案搬一站 |
| `addDay` / `dropDay` | 多加一天、縮成兩天 | 提案改天數 |
| `question` | 這裡有日文導覽嗎、附近有租車嗎 | 直接回答，不改行程 |
| `unknown` | 其他 | 說不會，並給三個可以按的例句 |

### 2.3 抽參數的規則

- **地名**：`BY_DEST` 的 name，加一組別名（花蓮縣→花蓮、台北市→台北、
  日月潭→sunmoonlake）。沒有模糊比對——猜錯城市比說不會更糟。
- **天數**：阿拉伯數字與中文數字都收（`三天`、`3 天`、`兩天一夜` 取較大者）。
- **興趣**：`INTEREST_LABELS` 的字，加同義詞（吃→food、拍照/打卡→photo、
  親子/帶小孩→family）。
- **景點**：對 `POIS` 的 `name` 做完全比對優先、包含比對次之。**同名多筆就問**，
  不自己選。
- **第幾天**：`第二天`、`Day 2`、`D2`、`明天`（相對於 `trip.today`）。

### 2.4 提案，不是直接動手

每一個會改變行程的意圖，回覆裡都帶一張 `Proposal`：

```ts
type Proposal =
  | { kind: "newTrip"; plan: Plan }
  | { kind: "addStop"; tripId: string; day: number; ref: StopRef }
  | { kind: "removeStop"; tripId: string; day: number; stopId: string }
  | { kind: "moveStop"; tripId: string; from: number; to: number; stopId: string };
```

畫面上是一張卡，寫清楚會發生什麼，兩顆按鈕：**套用** / **不要**。
按了才寫。這條規則跟文件那邊的第 4 點是同一條。

---

## 3. 對話畫面

`screens/Chat.tsx`，route `{ k: "chat"; tripId?: string }`。

- 訊息由下往上，使用者靠右、AI 靠左。沿用既有 `bg-surface` / `bg-brand-wash`，
  不做第二套氣泡樣式。
- **開場給三顆可以按的例句**，因為對著空白輸入框的人不知道能打什麼。
  帶 `tripId` 進來時例句換成改行程的（「第二天加一個夜市」）。
- 提案卡直接長在對話流裡。
- 輸入框固定在底部，`bottom nav` 上方。

**兩個入口**：

1. `一起規劃` → 新增一列「💬 跟 AI 說你想怎麼玩」，跟現有的
   「✨ AI 幫我排行程」並排。兩個背後同一個 planner。
2. 行程頁 → `✨ 行程有變？讓 AI 幫你改` → **「💬 直接告訴 AI」**。
   那一列現在是 `即將推出`，這一輪把它點亮，帶 `tripId` 進 Chat。

---

## 4. 旅行文件

`我的 → 🛂 旅行文件` 現在是一列死的「即將推出」。這一輪把它變成真的。

### 4.1 登機證是真的解析

登機證條碼是 **IATA BCBP**（Bar Coded Boarding Pass, Resolution 792）M 格式，
一個有規格的定長字串：

```
M1CHANG/TAI MR      EABC123 TPEHLNBR 0189 231Y012C0055 100
 │ │                 │      │  │  │   │    │  │   │
 │ │                 │      │  │  │   │    │  │   └ 座位
 │ │                 │      │  │  │   │    │  └ 艙等
 │ │                 │      │  │  │   │    └ 儒略日（一年第幾天）
 │ │                 │      │  │  │   └ 航班號
 │ │                 │      │  │  └ 航空公司
 │ │                 │      └──┴ 起降機場
 │ └ 旅客姓名        └ 訂位代號
 └ 幾段行程
```

`lib/bcbp.ts` 真的把它拆開。這不是猜的，是照規格走的定位切字。

### 4.2 條碼怎麼進來

`lib/scan.ts`：用 **zxing-wasm** 解 PDF417（登機證）、QR（飯店）、Aztec（火車票）。

- **動態 import**，不進初始 bundle。使用者不開文件頁就永遠不載那顆 wasm。
- 解不出來就退回手動輸入，不是失敗畫面。
- README 那句「沒有新增任何 runtime 相依」要改掉——這是第一個，而且是有理由的：
  沒有解碼器就沒有「真解析」，而第 3 點決定了要真解析。

### 4.3 飯店 QR 沒有標準

飯店的 QR 可能是網址、Wi-Fi 設定、訂房代號，甚至只是一串流水號。
**所以不猜。** 讀到什麼就顯示什麼，然後讓使用者自己補飯店名、入住日、房號。
硬把一串亂碼說成「桑母樂飯店」是這個專案一直在避免的那種謊。

### 4.4 資料放哪

```ts
interface TravelDoc {
  id: string;
  kind: "flight" | "hotel" | "other";
  addedAt: number;
  /** 原始掃出來的字串。永遠留著，因為解析可能是錯的。 */
  raw: string;
  /** BCBP 解析結果，只有 flight 有 */
  flight?: { passenger; pnr; from; to; carrier; flightNo; date; cabin; seat };
  /** 使用者自己填的 */
  manual?: { title?; date?; note? };
  /** 原圖，dataURL。只存在這台裝置 */
  image?: string;
}
```

`resomap_docs`，走既有的 `lib/persist.ts`（帶版本戳）。
Demo 重置一併清掉，跟 trips 同一條規則。

**隱私**：這個 app 沒有後端，所以文件不可能被上傳——這不是承諾，是架構事實。
畫面上會寫「只存在這台裝置」，並且提供單筆刪除。

### 4.5 範例檔

內建一張偽造登機證：`MICKEY/DEMO MR`、`BR 189`、`TPE→HLN`。
一鍵載入，簡報時不用掏自己的機票，也能證明解析是真的。

---

## 5. 文件回饋到行程（要先問）

| 讀到什麼 | 提案 |
|---|---|
| 航班 10:35 落地 TPE | 「Day 1 的行程要改成中午之後開始嗎？」 |
| 航班日期 8/20 | 「要把行程的第一天對到 8/20 嗎？」 |
| 飯店名稱 + 入住日 | 「要把它放進那一天的住宿嗎？」 |

一律是一張提案卡加兩顆按鈕。**不會自己動。**

---

## 6. 明確不做

- 不接任何模型、不呼叫任何 AI API、不放 API key
- 不加第五個 tab
- 不動探索頁、導覽庫、卡片樣式、品牌色
- 不刪五步驟精靈
- 文件不上傳到任何地方（也沒有地方可以上傳）
- 飯店 QR 不猜內容
- 對話聽不懂時不硬掰

---

## 7. 驗收

| Flow | 路徑 |
|---|---|
| A | 一起規劃 → 跟 AI 說 →「花蓮三天兩夜」→ 提案 → 套用 → 行程分頁多一筆 |
| B | 對話續問「多一點美食」→ 重新提案 → 套用 |
| C | 行程 → Day 2 → AI 幫你改 → 直接告訴 AI →「加七星潭」→ 套用 → 那一站在 Day 2 |
| D | 對話「把松園別館拿掉」→ 套用 → 那一站消失 → 重新整理還是消失 |
| E | 對話打一句亂碼 → 說不會 → 給三個可按的例句 |
| F | 我的 → 旅行文件 → 載入範例 → 讀出 MICKEY/DEMO・BR189・TPE→HLN |
| G | 上傳一張飯店 QR → 讀出原始內容 → 手動補名稱 → 存起來 |
| H | 文件頁「對到行程」→ 提案 → 套用 → Day 1 時間改了 |
| I | Demo 重置 → 文件清空 |

加上回歸：雨天改行程、AI 重排、抵達偵測、分帳、多型 Stop 的五種站別。
375 / 390 / 430 無 overflow，無 console error，無破圖。

---

## 8. 順序

1. 對話引擎 + 測試（沒有 UI，先讓它答對）
2. 對話畫面 + 產新行程（Flow A / B / E）
3. 對話改現有行程（Flow C / D）
4. BCBP 解析 + 測試
5. zxing 掃描 + 文件畫面 + 範例（Flow F / G / I）
6. 文件回饋行程（Flow H）
7. 文件、README、FEATURE_MATRIX、INTEGRATION_PLAN §24

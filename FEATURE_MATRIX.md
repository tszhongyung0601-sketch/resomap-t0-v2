# FEATURE MATRIX

`resomap-t0-demo`（T0）· `resomap-http-demo`（HTTP）· `resomap-t0-v2`（V2）

**讀法**：V2 欄位寫的是「這一版怎麼處理」，不是「有沒有」。
凡是 T0 已經有的，V2 一律以 T0 的實作為準，只補 HTTP 有而 T0 缺的 capability。

---

## 1. 骨架與導航

| Feature | T0 | HTTP | V2 |
|---|---|---|---|
| 首頁 Homepage | ✅ 地圖 + 導覽 rail + 行程卡 + 九宮格 | ❌（是簡報頁） | **完全保留 T0**。section 數量、順序、視覺零改動；只有按鈕目的地可以接新功能 |
| Bottom navigation | ✅ 探索 / 導覽庫 / 行程 / 一起規劃 | 探索 / 導覽 / 周邊 / 我的 | **保留 T0 四個 tab**，不新增 item。新功能掛在景點與「我的」底下 |
| 手機外框 / 響應式 | ✅ `AppShell` <520px 全螢幕、≥520px 393×852 外框 | 固定 `.phone-frame` + 右側面板 | **沿用 T0**。HTTP 的右側控制面板（快速導覽 / 報告摘要 / Mockup Gallery）**整個刪除** |
| Design system | ✅ tokens + `ui.tsx` 15 個元件 | 自成一套 CSS（橘 #f47b20 / 藍 #2b87b9） | **只有一套：T0**。新元件全部建在 `ui.tsx` 之上 |
| 路由 | ✅ 記憶體 route stack，無 router | `state.view` switch | **沿用 T0**，新增 9 個 route |
| i18n | ✅ 九語，繁中原文即 key | ❌ | 沿用；V2 新畫面的文案暫未進字典（缺字自動 fallback 繁中） |

## 2. 地圖與景點

| Feature | T0 | HTTP | V2 |
|---|---|---|---|
| 地圖 | ✅ Leaflet + OSM + 自寫叢集 | 假背景圖 + 絕對定位的 pin | **保留 T0 地圖**，不採用 HTTP 的 |
| 首頁地圖在看什麼 | 台灣全島錨點 / 目前行程的停留點 | ❌ | **改成位置感知**：以旅客所在地為中心，畫附近可以聽的景點 |
| 目前位置 | ❌ | ❌ | **新增**藍點 + 白框 + 半透明精準度圈。真實定位用 `coords.accuracy`，Demo 位置用 45 m，畫面不寫死任何精準度數字 |
| 定位權限 | ❌ | ❌ | **只在按下定位鈕時**呼叫 `getCurrentPosition`（`enableHighAccuracy` / 8s / 30s）。進站不要權限，失敗不 `alert()`，留在 Demo 位置並吐一句話 |
| 附近景點 | ❌ | ❌ | **新增七個新店 / 景美景點**，座標開發階段從 OSM 查得。全部有語音導覽，全部有 Commons 真照片 |
| 景點 marker | 白底 emoji / 有無故事雙色 | 絕對定位方塊 | **新增橘色耳機 marker**（SVG，40px，選取 48px + 光暈）。不用 🎧 emoji |
| Marker → 預覽卡 | 選取卡（無照片） | ❌ | **新增底部預覽卡**：照片 / 名稱 / 距離 / 有語音導覽 / 一句話 / 開始導覽 + 查看景點。不瞬間跳頁 |
| 地圖搜尋 | 導向全站搜尋 | ❌ | **改搜這七個景點**，選了就 flyTo + 選取 + 開卡片；最後一列仍可進全站搜尋 |
| OSM POI 混合 | ❌ | ❌ | **新增** `lib/overpass.ts`：成功就合併最多 3 個小灰點，失敗一律靜默回空陣列 |
| Spot pin 區分有無語音 | ✅ 僅首頁（`hasStory`） | ✅ 藍 / 黑 | **擴充到地圖分頁**：pin 用 `hasAudio`（含旅人上傳），附圖例 |
| 付費 pin 標示 | ✅ 虛線環 | ❌ | 修正：有色 pin 也會套用虛線環（原本會被吃掉） |
| Map → 預覽 → 詳情 → 語音 | 部分（預覽 → 詳情） | ✅ | **補齊**：地圖選取卡新增「探索附近」，詳情頁有語音區塊 |
| 景點頁 | ✅ 故事卡 / 情境圖 / 導航 / 門票 / 在地優惠 / 附近 / 加入行程 | 名稱 + 地址 + 封面 + 語音清單 | **保留 T0 全部**，新增「語音導覽」區塊與「探索附近」按鈕 |

## 3. 語音導覽

| Feature | T0 | HTTP | V2 |
|---|---|---|---|
| ResoMap 自製導覽 | ✅ 15 篇，30 秒 / 完整雙版本 | ❌ | 保留，`data/stories.ts` 仍是正典 |
| 一個景點多則語音 | ❌（一景點一篇） | ✅ | **新增** `AudioGuide` 模型，`lib/audio.ts` 在讀取時把 Story 併進清單 |
| 多語語音 | 部分（語言標籤，只有中文出聲） | ✅ 六語清單 | **新增**：每則語音有自己的語言與文稿，播放器用 BCP-47 交給瀏覽器，裝置有語音就唸，沒有就字幕模式 |
| 語音清單搜尋 | ❌ | ✅ | **新增**（標題 / 提要 / 錄音者 / 語言） |
| 付費商家置頂語音 | ❌ | ✅ 2 則 | **新增**，上限 2 由 `lib/audio.ts` 強制，標「★ 店家精選」＋內容類型 |
| 播放器 | ✅ 進度 / seek / ±10s / 字幕高亮 / 語言 / 收藏 / 分享 | 基本進度條 | **擴充同一個播放器**（不做第二套）：新增 不推、留言、播完 CTA |
| 播完 → 探索附近 | ❌ | ✅ | **新增「接下來想做什麼？」五選一**（看附近美食 / 找伴手禮 / 找包車 / 找私人導遊 / 找 Local tour），各自帶 5km 內筆數並直接進該分類清單；只在真的播完才出現，且只列出真的有東西的選項 |
| 讚 / 不推 / 留言 | 收藏 ✅ | 顯示數字（不可操作） | **新增可操作**，`lib/reactions.ts` + localStorage，讚與不推互斥 |
| 新增語音（＋） | ❌ | ✅ 兩步 | **新增六步流程**：音檔 → 語言 → 標題 → 景點 → 說明 → 送審。檔案選取是真的 `<input type=file>`，但不上傳 |

## 4. 周邊推薦

| Feature | T0 | HTTP | V2 |
|---|---|---|---|
| 周邊推薦頁 | ❌ | ✅ | **新增**，T0 style 重做成**圖片型 Category Card**，標題是旅客的問題（吃什麼？／帶什麼回家？／需要人帶你玩？／更多旅遊體驗／今晚住哪？）而不是七個後台選單列 |
| 5km / 10km | ❌ | ✅ | **新增**，用 T0 的 `Segmented`；每一列即時顯示該範圍的筆數 |
| 站內 / 聯盟分組 | ❌ | ✅ | **改成低調揭露**：不再用「ResoMap 付費服務」當標題，改成每張卡底部一行 11px 灰字（`ResoMap 精選` / `聯盟合作 · Klook / KKday`），完整說明放頁尾 |
| 空清單處理 | — | 會開到空頁 | 卡片寫「看更遠」且**仍可點**，進去說明是「這個範圍沒有」還是「這個國家還沒開」，前者提供「看 10 公里內」 |
| 覆蓋率 | — | 只有景美夜市一個地點 | 54 個台灣景點**全部**在 10km 內七類全滿，45 個在 5km 內全滿 |
| 排序邏輯 | ❌ | 只有文字說明 | **真的實作** `rankNearbyServices()`：距離 40 / 評價 25 / 付費曝光 15 / 審核 10 / 相關度 10，純函式、無 AI、無隨機 |
| 排序透明度 | ❌ | ❌ | **「推薦排序 ⓘ」兩句話**：依距離、評價與相關度綜合排序；付費曝光會標示。精確權重留在 `WEIGHTS`，不印在旅客螢幕上 |
| 數量單位 | — | 一律「X 家」 | `getNearbyCountLabel()` / `getNearbyCountUnit()`：餐廳・伴手禮＝家、住宿＝間、司機・導遊＝位、行程＝個行程 |

## 5. 商家

| Feature | T0 | HTTP | V2 |
|---|---|---|---|
| 商家資料 | 只有「在地優惠」deal（即將推出） | ✅ 3 筆 | **64 筆**，真座標，涵蓋台灣八城；含未訂閱與審核中的例子 |
| 周邊列表卡（商家 / 司機 / 導遊 / Local tour / 旅館） | — | 一次塞滿九行資訊 + 3 顆 CTA | **統一骨架**：96px 方形縮圖在左、名稱＋推薦夥伴徽章、副標（區域/工作室・距離）、語言 chips、星等、三格規格表、三顆 CTA。司機與導遊另有自我介紹與主題 chips。**沒有資料的格子不出現**——商家沒有價格就不放價格格。規格見 [CARD_SPEC.md](CARD_SPEC.md) |
| 商家詳情 | ❌ | Modal | **新增整頁**：營業時間、地址、語言、會員狀態、店家語音、評價、地圖 |
| 導航前往 | ✅（景點） | Modal 模擬 | **真的開 Google Maps**（帶座標） |
| 立即聯絡 | ❌ | Modal | **聯絡 Sheet**：有 URL 真的開，沒有的列出來並說明 |

## 6. 專業服務者（包車 / 導遊）

| Feature | T0 | HTTP | V2 |
|---|---|---|---|
| 包車司機 | ❌ | ✅ 3 筆 | **19 位**，與導遊共用 `Provider` 模型與畫面 |
| 私人導遊 | ❌ | ✅ 3 筆 | **20 位** |
| 服務者列表卡 | — | 一次塞滿全部資料 | **與商家共用同一個骨架**（見 §5）：96px 方形人像 / 名稱＋推薦夥伴 / 服務區域或工作室・距離 / 語言 chips / 星等・服務趟數 / 價格區間・服務時段・車型或服務類型 / 自我介紹 / 主題 chips + 3 顆 CTA |
| 服務者詳情 | ❌ | Modal | **新增整頁**：三個統計、自介、服務內容、車型座位或服務類型、招牌路線或導覽主題、評價、聯絡管道 |
| ResoMap 推薦夥伴 | ❌ | ✅（無規則） | **新增，且有規則**：`isPaid && reviewStatus === "approved"`，是推導不是欄位；規則印在每一頁 |
| 身份擇一 | ❌ | ✅（state 切換） | **由結構保證**：`account.professionalRole` 是單一欄位，導遊與包車不可能同時成立 |
| 商家 vs 專業身份 | ❌ | 同一個 state | **拆開**：`merchantMembership` 與 `professionalRole` 互不影響。啟用商家不會關掉導遊 |
| LINE / WhatsApp / 電話 / 預約 | ❌ | Modal | **支援真實 URL**，資料驅動 |

## 7. 聯盟 / OTA

| Feature | T0 | HTTP | V2 |
|---|---|---|---|
| Affiliate 模型 | ✅ `AffiliateProduct`（門票比價）+ `Deal` | 硬寫在畫面上 | **新增 `AffiliateOffer`**：`partner / productId / affiliateUrl / trackingId / commissionType / commissionRate` |
| Booking / Agoda / Trip.com | ✅ 住宿 deal | ✅ | **新增周邊住宿清單** |
| Klook / KKday | ✅ 門票 deal | ✅ | **新增 Local tour 清單** |
| Local tour / 旅館詳情頁 | ❌ | ❌ | **新增整頁**：大圖 / 名稱 / 平台評分（Booking 用 10 分制，不換算成星） / 價格 / 平台 / 介紹 / 「預訂與退改都在平台完成，ResoMap 不經手金流」/ 地圖 / 收藏 + 前往。`affiliateUrl` 是空的，按鈕讀欄位並說明 |
| 收藏 offer | ❌ | ❌ | **新增**：`lib/saved.ts` 從兩個集合擴成三個（景點 / 導覽 / 平台商品） |
| 外連行為 | 模擬 Sheet | Modal | **資料驅動**：`affiliateUrl` 有值就帶 tracking id 真的開；目前全空 → 出說明 Sheet |
| Funnel 追蹤 | ✅ `lib/track.ts` | ❌ | 沿用；聯盟點擊也寫進同一個 funnel |
| 揭露 | ✅ `AFFILIATE_DISCLOSURE` | 部分 | 沿用同一句，貼在每一個聯盟清單底部 |

## 8. 評價

| Feature | T0 | HTTP | V2 |
|---|---|---|---|
| 導覽評分 | ✅ 由 likes/plays 推導 | ❌ | 保留 |
| 商家 / 服務者評價 | ❌ | Modal（一段文字） | **新增評價頁**：平均分、總則數、長條圖、逐則列表 |
| 評價入口 | ❌ | ✅ 點星等 | **新增**：點「4.9（128 則評價）」進入 |
| 樣本 vs 總數誠實度 | — | 4.8 / 86 則但只顯示 3 則 | **改進**：畫面明說「最近 N 則」、長條圖統計的是這 N 則 |

## 9. 訂閱與會員

| Feature | T0 | HTTP | V2 |
|---|---|---|---|
| 訂閱方案頁 | ❌ | ❌（只有概念） | **新增**：一般 / 商家 / 導遊 / 包車 四種 |
| 價格 | ❌ | ❌ | **一律「價格待確認」**。codebase 沒有定價，不自行發明；`priceTwd: null` |
| 方案內容 | ❌ | 報告文字 | **新增**，且每個方案列出「訂閱之後會出現在哪裡」，每一列都是這個 build 裡真的存在的畫面 |
| 專業會員頁 | ❌ | ✅ | **新增**：身份切換、公開資料編輯、推薦狀態、我上傳的語音 |
| 資料可編輯 | ❌ | ✅ Modal | **新增**，用 T0 `Sheet`，欄位標示會出現在哪裡 |
| 商家 vs 專業身份 | ❌ | 混在一起 | **分開**：商家是 Business Account，包車 / 導遊是 Personal Service Provider Identity，資料結構上互不干涉 |

## 10. 行程（T0 既有，V2 串通）

| Feature | T0 | HTTP | V2 |
|---|---|---|---|
| 行程列表 / 總覽 / 時間軸 | ✅ | ❌ | 完全保留 |
| 拖拽編輯 / AI 重排 / 共識 / 記帳分帳 | ✅ | ❌ | 完全保留 |
| Stop → 語音 | ✅「聽故事」 | ❌ | **改為「開始語音導覽」**（需求指定字樣），且沒有 ResoMap 故事但有旅人上傳的地點也會出現 |
| Stop → 播完 → 探索附近 | ❌ | ❌ | **新增**：那一列換成「探索附近」，狀態讀自 `story_finish` 事件 |

## 11. 明確不移植

| HTTP Demo 的東西 | 為什麼 |
|---|---|
| 右側控制面板（快速導覽 / 報告摘要 / Mockup Gallery） | V2 是產品，不是簡報 dashboard |
| `styles.css` 全部 | 一個專案只能有一套 design system |
| `assets/mockups/*`、`assets/current/*` | 報告用圖，產品裡沒有位置 |
| 「景美夜市」作為新 POI | T0 的 79 個座標都經過驗證。夜市內容改掛在 `饒河街觀光夜市`（真座標），多語導覽掛在 `龍山寺` 與 `大稻埕迪化街` |
| 底部導航改成 探索 / 導覽 / 周邊 / 我的 | T0 的導航是 source of truth |

## 12. 圖片

| 用途 | 來源 | 處理 |
|---|---|---|
| 景點 | T0 既有 53 張 CC 授權真照片 | 沿用，`PhotoCredit` 標出處 |
| 商家（旅館與部分餐廳） | **T0 真照片**，借用該店所在街道的那一張 | `photoFromPoi`，詳情頁顯示「周邊實景照片：作者 / 授權」 |
| 商家（土產店、部分餐廳） | HTTP Demo `assets/content/*.png` | `scripts/build-demo-photos.mjs` → webp card 720 / hero 1280 |
| 司機・導遊 | **40 位一人一張真人照片**（Pexels 圖庫模特兒），`public/portraits/`（16:9 兩尺寸 + 1:1 縮圖） | `scripts/fetch-stock-portraits.mjs`：依選角搜圖、照片與攝影師雙重去重、擋掉紀實街拍與時尚棚拍、亮度過濾。角落標「示意」、詳情頁標「非實際服務者本人」＋攝影師，頁尾再說一次。攝影師名單見 [PORTRAIT_ATTRIBUTION.md](PORTRAIT_ATTRIBUTION.md)。**不借景點風景照** |
| 周邊分類卡 | 兩者混用（夜市小吃 / 伴手禮店 / 導遊 / 司機 / 九份 / 礁溪 / 北投） | 導遊與司機那兩張改指向真實的 provider 人像（`portraitOf`），同樣標「圖庫示意」 |
| 找不到合適照片時 | T0 `Generated` 硬邊海報圖 | 不是 emoji placeholder，是設計過的圖 |

原始八張 PNG 共 18 MB（1254×1254），轉檔後 1.1 MB。全部 `loading="lazy"`，
載入失敗自動降級成海報圖，所以不可能出現 broken image。

## 13. Performance

| 項目 | 前 | 後 |
|---|---|---|
| 初始 JS | 單一 chunk 916 kB | entry 250 kB + react 174 kB + leaflet 159 kB（可分別快取） |
| 其餘 | — | 40 個按需 chunk，點到才載 |
| 圖片 | — | 全部 `loading="lazy"` + webp + 兩種尺寸 |
| 相依套件 | leaflet / react-leaflet | **沒有新增任何 runtime 相依**（sharp 只在建圖腳本，devDependency） |

## 14. 第二輪 Polish（工程說明退場）

| 原本印在產品畫面上的 | 現在 |
|---|---|
| 距離 40%／評價 25%／付費曝光 15%／審核 10%／相關度 10% | 「推薦排序 ⓘ」兩句話；權重留在 `WEIGHTS` |
| 「這個 Demo 沒有訂閱定價，金額之後只改一個檔案」 | 「價格待確認」＋「正式方案與價格將於合作開放時公布。」 |
| 每頁一段「付費＋審核＋排名加權」的推薦夥伴規則 | 標章旁一顆 ⓘ，兩句話 |
| 「Demo 版本沒有後端／沒有金流／只存在這台裝置上」×5 處 | 我的 →「關於這個 Demo」一處，加各頁一行短註 |
| 「資料裡的 affiliateUrl 是空的，串上 tracking id 之後…」 | 「合作平台的預訂連結尚未開通。」 |
| 「XXX 走路五公里內的選擇」 | 「XXX 5 公里內的推薦」（5 km 是搜尋半徑，不是步行距離） |

## 15. 兩處刻意的取捨

| 決定 | 理由 |
|---|---|
| 地圖分頁的 pin 用 `hasAudio`，首頁的 pin 維持 `hasStory` | 首頁視覺 100% 保留是最高規則。兩張地圖的圖例文字因此不同（「有語音故事」vs「有語音導覽」），兩句話都為真 |
| `MapTab` 的「更多篩選」四項維持「即將推出」 | 那是**地圖圖層**篩選，V2 沒有做商家 pin 圖層。點亮一個點不出東西的按鈕比留著誠實的標籤更糟 |

## 16. V3：行程升級（第六輪）

| 功能 | 之前 | 現在 |
|---|---|---|
| 行程可以放什麼 | 只有景點 | 景點／餐廳／導遊／一日遊／租車，全部一等公民 |
| 行程存不存 | refresh 就消失 | `resomap_trips` + `resomap_day_edits`，Demo 重置清掉 |
| 附近租車 | 沒有 | 周邊推薦第八個分類，9 城市 22 個據點，每個據點一張車輛照片 |
| 「租車・接送」畫面 | 篩標題含「租車」的 deal | 跟周邊推薦讀同一份 `carRentals.ts` |
| 景點頁「加入行程」 | 直接丟到 `today`，不問 | Day 選擇 sheet（跟導覽庫同一支） |
| 加入後的 toast | 「已加入 Day 2」 | 「✓ 七星潭已加入 Day 2」 |
| 導覽庫卡片 | 只有圖＋標題可點 | 整張卡可點，兩個動作按鈕維持自己的目標 |
| AI 排行程 | 沒有 | 一起規劃 → 五步驟 → 真的從 87 個地點選 |
| 日月潭 | 只是個地名 | 4 景點 · 4 商家 · 2 服務者 · 4 聯盟商品 · 1 篇導覽 |
| 台北 101 | 有照片有門票，沒有導覽 | 有中文語音導覽 |

### 資料量

| | 之前 | 現在 |
|---|---|---|
| 景點 | 87 | 91 |
| 語音導覽 | 22 | 24 |
| 有照片的景點 | 86 | 90 |
| 商家 | — | +4 |
| 服務者 | — | +2 |
| 聯盟商品 | — | +4 |
| 租車據點 | 0 | 22 |
| 車輛照片 | 0 | 22（Pexels，全部標「示意」） |

### 商業揭露

真實品牌一律標「Demo・未正式合作」，而且是**每張卡一次**，不是每頁一次：
列表卡片、加入行程的 sheet、行程裡那一列、詳情頁 above the fold。
只寫在頁尾的揭露，在被截圖的時候不存在。

`CarRental` 沒有 `isPaid` 也沒有 `reviewStatus` 欄位可以設，所以結構上就拿不到
推薦夥伴標章。這比「記得不要設成 true」可靠。

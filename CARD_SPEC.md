# 周邊推薦卡片改版規格

把商家、包車司機、私人導遊、Local tour、旅館五種卡片，改成同一個骨架：
**左方形縮圖 + 右資訊 / 三格規格 / 三顆 CTA**。

底下每一條都對應一個已經拍板的決定，不是提案。

---

## 1. 骨架

一張卡由上而下四段，寬度是內容寬（393 螢幕扣掉左右 20 = 353）：

```
┌──────────────────────────────────────────────┐
│ ┌────────┐  名稱  ★ ResoMap 推薦夥伴          │  ← 上半段整塊可點 → 詳情頁
│ │        │  工作室 · 2.1 km                   │
│ │  96×96 │  中文  English  日本語              │
│ │        │  ★ 4.9（128 則評價）· 1,258 趟     │
│ └────────┘                                   │
│                                              │
│ ┌────────┐┌────────┐┌────────┐                │  ← 三格規格
│ │價格區間││服務時段││車型    │                │
│ │NT$2,500││07:00–  ││TOYOTA  │                │
│ │–6,500  ││21:00   ││Alphard │                │
│ └────────┘└────────┘└────────┘                │
│                                              │
│ 自我介紹：從事包車服務超過八年……              │  ← 只有司機 / 導遊
│                                              │
│ 九份老街  十分瀑布  宜蘭礁溪                  │  ← 只有司機 / 導遊
├──────────────────────────────────────────────┤
│ [ LINE 聯絡 ]  [ 立即預約 ]  [ 查看詳情 ]     │  ← 三顆，並排等寬
└──────────────────────────────────────────────┘
```

尺寸：縮圖 96×96 / radius 12；縮圖與右欄間距 12；三格 grid 3 欄 gap 8，
每格 `bg-surface-2` radius 10，標籤 10.5px `ink-3`、值 12.5px semibold `ink`；
CTA 高 44、radius 999、gap 8。

**巢狀 button 禁止**：上半段是一顆 `<button>`，三顆 CTA 是它的兄弟，
外層是 `<article>`。這是 T0 既有的硬規則，不因為版面改了就放寬。

---

## 2. 每種卡放什麼

| | 縮圖 | 副標 | chips 列 | 三格 | 自我介紹 | 主題 chips |
|---|---|---|---|---|---|---|
| 包車司機 | 人像 1:1 | 服務區域 · 距離 | 語言 | 價格區間 / 服務時段 / 車型 | ✅ | 招牌路線 |
| 私人導遊 | 人像 1:1 | 工作室 · 距離 | 語言 | 價格區間 / 服務時段 / 服務類型 | ✅ | 導覽主題 |
| 商家 | 街景方裁 | 分類 · 區域 · 距離 | 語言 | 分類 / 營業時間 / 語言 | ❌ | ❌ |
| Local tour | 情境方裁 | 平台 · 距離 | — | 價格 / 平台 / 出團單位 | ❌ | ❌ |
| 旅館 | 情境方裁 | 平台 · 距離 | — | 價格 / 平台 / 計價單位 | ❌ | ❌ |

**沒有資料的格子不出現**，不寫「—」也不編數字。商家沒有價格，所以商家沒有價格格；
129 家裡只有 24 家有優惠，所以優惠不當第三格。

---

## 3. 三顆 CTA

中間那顆是實心橘，左右兩顆描邊——照截圖。

| | 左 | 中（實心橘） | 右 |
|---|---|---|---|
| 司機 / 導遊 | LINE 聯絡 | 立即預約 | 查看詳情 |
| 商家 | LINE 聯絡 | 導航 | 查看詳情 |
| Local tour / 旅館 | 收藏 | 前往 Klook | 查看詳情 |

- **立即預約**：`contact.bookingUrl` 全部是空的（40 位都沒有），所以它開既有的
  `DemoLinkSheet` 說明「預訂連結接上之後這顆按鈕會怎麼運作」。這不是 dead CTA，
  是 V2 一開始就定的誠實作法，詳情頁那顆本來就這樣。
- **前往 Klook**：平台名依資料變（Klook / KKday / Booking / Agoda）。
  `affiliateUrl` 是空的，同樣走說明 sheet。
- **收藏 / 查看詳情**：這兩顆今天不存在，見第 5 節。

### 這裡違反了一條 T0 硬規則，寫下來

T0 的規則是「一個畫面只能有一顆實心橘按鈕」。一列五張卡就是五顆。
這是你看過截圖後選的，代價是往下滾時橘色一直出現、畫面焦點會散。
要收回來的話最小改動是把中間那顆改成橘色描邊，其餘不動。

---

## 4. 顏色

截圖的藍色標題與藍色人名**不採用**。標題用 T0 的 `ink` 黑字，
人名同樣是 `ink` bold。理由是 V2 只有一套色系，加藍會跟橘色 header、
橘色按鈕、橘色 tab 打架。

「LINE 聯絡」也不用 LINE 綠，用 T0 的描邊樣式。
理由一致：Klook / Booking 在這個 app 裡也是純文字，不帶各自的品牌色。
（想要綠色的話這是一個 class 的事。）

---

## 5. 這一版要新增的東西

1. **人像重畫成 1:1**。現在的 40 張是 16:9 構圖，硬裁方形會把人切到一邊。
   `draw-portraits.mjs` 加一個方形模式：人置中、背景收窄、頭仍在上三分之一，
   輸出 `-thumb.webp` 320×320。詳情頁的 16:9 大圖照舊。
2. **Local tour / 旅館詳情頁**（新畫面 + 新 route）。
   商家跟服務者都有詳情頁，只有平台商品沒有——這本來就是缺口。
   內容：大圖 / 名稱 / 星等（Booking 是 10 分制）/ 價格 / 平台 /
   介紹 / 揭露說明 / 前往。
3. **收藏可以收 offer**。`lib/saved.ts` 目前只存 `pois` 與 `stories`，加一個 `offers`。

**沒有做的**：把 Local tour「加入行程」。行程的 `Stop` 只認 `poiId`，
要讓它存行程商品會動到時間軸、路線計算、地圖 pin 與分帳——那是資料模型改，
不是版面改。所以那顆按鈕換成收藏。

---

## 6. 不動的

首頁、地圖、bottom nav、播放器、語音清單、訂閱與專業會員、行程、
排序邏輯與推薦夥伴規則、`resomap-t0-demo`、`resomap-http-demo`。

卡片上半段點下去進的還是同一個詳情頁，沒有第二套。

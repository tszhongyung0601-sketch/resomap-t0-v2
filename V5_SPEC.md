# ResoMap V5 — 行程變成主場，第四個 tab 變成通路

一次 IA 重組。**沒有新功能**——所有東西都已經存在，這一輪決定的是它們住在哪裡。

品牌色、橘色 header、字體、手機外框、卡片樣式、圓角、間距、既有圖片、
**探索首頁**、導覽庫全部不動。Bottom nav 維持 **4 個**。

---

## 0. 七個決定

| # | 決定 |
|---|---|
| 1 | 行程 tab 改成**分頁**：行程 / 文件 / 共同規劃 |
| 2 | 更多優惠 tab **先分來源，再分類別** |
| 3 | AI 入口**只留對話**，五步驟精靈收掉 |
| 4 | 探索首頁那排「旅程服務」**完全不動** |
| 5 |（自訂）精靈**整支刪掉**，不留死程式碼 |
| 6 |（自訂）我的 →「旅行文件」那一列**保留**當捷徑 |
| 7 |（自訂）第四個 tab 就叫**「更多優惠」** |

---

## 1. Bottom nav

```
探索        導覽庫        行程                    更多優惠
Compass    Headphones   Route                   Tag（新圖示）
不動        不動          三個分頁                 一起規劃搬走後的位置
```

**仍然是四個。** V3 那條「不要改成 5 個、不要新增『我的』」的規矩沒有被破——
換掉的是第四個 tab 的內容，不是數量。

`Tab` 型別 `"together"` → `"deals"`。這是一次 rename，不是新增。

---

## 2. 行程 tab：三個分頁

`Segmented` 已經存在（周邊推薦的 5km / 10km 在用），直接沿用，不做第二套分頁元件。

### 2.1 行程（預設）

現在的清單，**一列都不改**。打開 tab 第一眼還是自己的行程。

清單上方多一列入口：**「💬 跟 AI 說你想怎麼玩」**。

### 2.2 文件

`screens/Documents.tsx` 的內容原地搬進來。拆成：

- `DocumentsPane` — 沒有 `Screen` / `TopBar`，給分頁用
- `Documents` — 保留 route，包一層 `TopBar`，給「我的 → 旅行文件」用

同一份程式碼兩個入口，不是複製兩份。

### 2.3 共同規劃

`screens/Together.tsx` 的內容原地搬進來，一樣拆成 pane。
房間、我的偏好、大家想去的地方、AI 幫大家排行程，全部照舊。

**「一起規劃」這四個字不會消失**，它變成這個分頁的名字。

---

## 3. 更多優惠 tab

先分來源，再分類別。**因為這兩群的性質完全不同，而讀的人有權知道差別。**

```
┌ ResoMap 合作商家 ─────────────────────────┐
│ 68 家商家、42 位包車/導遊                    │
│ 有審核制度、有「推薦夥伴」標章                  │
│ 依類別：餐廳 / 伴手禮 / 住宿 / 包車 / 導遊      │
└──────────────────────────────────────┘

┌ 聯盟合作平台 ─────────────────────────────┐
│ Klook・KKday・Booking・Agoda・Trip.com     │
│ affiliateUrl 全空，按鈕會說明會發生什麼        │
│ 依類別：門票 / 住宿 / 交通 / 租車 / 機票 /     │
│         eSIM / 旅平險                       │
└──────────────────────────────────────┘

┌ 租車 ───────────────────────────────────┐
│ iRent・和運・格上 + Klook/KKday/Trip.com    │
│ 每一張都標「Demo・未正式合作」                 │
└──────────────────────────────────────┘
```

**資料全部是現成的**：`MERCHANTS`、`PROVIDERS`、`AFFILIATE_OFFERS`、
`AFFILIATE_PARTNERS`、`CAR_RENTALS`、`DEALS`、`SERVICES`。
這一頁是把它們組起來，不是發明新的。

既有的 `Deals` 與 `Services` 兩個畫面**保留**，這個 tab 是它們的上層目錄——
探索首頁那排九個圖示還是指向它們，兩邊不會打架。

### 商業揭露維持既有紀律

- 「ResoMap 推薦夥伴」＝付費 ✚ 通過審核，兩個都要
- 聯盟平台：`affiliateUrl` 全空，按鈕讀欄位、說明會發生什麼
- 租車：每張卡「Demo・未正式合作」
- 不新增任何「合作」字樣，不暗示任何實際上不存在的關係

---

## 4. AI 精靈收掉

`screens/AiPlanner.tsx`（V3 的五步驟）**整支刪除**，連同 `{ k: "aiPlan" }` route。

理由：對話能做它做的每一件事，而且能做更多（改現有行程）。
留著一支沒有入口的畫面就是死程式碼，這個專案剛刪掉情境圖就是為了同一個理由。

**`CreateTrip` 不動** ——那是 T0 的「建立空行程」精靈，四個入口共用，跟這支不是同一件事。

`lib/planner.ts` **留著**，對話在用它。

---

## 5. 明確不做

- 不加第五個 tab
- 不動探索首頁（一個 section、一個 pixel 都不動）
- 不動導覽庫、卡片樣式、品牌色
- 不刪 `CreateTrip`、不刪 `Deals`、不刪 `Services`
- 不發明新的合作關係、不新增「合作」字樣
- 不為了重構而重構

---

## 6. 驗收

| Flow | 路徑 |
|---|---|
| A | 行程 tab → 三個分頁都在，預設停在「行程」，清單跟以前一樣 |
| B | 行程 → 跟 AI 說 →「花蓮三天兩夜」→ 套用 → 回行程分頁看得到 |
| C | 行程 → 文件 → 載入範例登機證 → 讀出 BR 189 → 對到行程 |
| D | 行程 → 共同規劃 → 房間、偏好、投票、AI 共識全部照舊 |
| E | 更多優惠 → 兩群分開、每群依類別、標章與揭露都在 |
| F | 更多優惠 → 任一 OTA →「前往 Klook」→ 說明尚未開通 |
| G | 探索首頁 → 九個服務圖示 → 全部照舊 |
| H | 我的 → 旅行文件 → 一樣打得開 |
| I | Demo 重置 → trips / edits / docs 三個 key 都清掉 |

加上回歸：雨天改行程、AI 重排、抵達偵測、分帳、多型 Stop 五種站別、
對話改行程、登機證解析。

375 / 390 / 430 無 overflow，無 console error，無破圖。

---

## 7. 順序

1. `Tab` rename（together → deals）＋ 新圖示
2. 行程 tab 三分頁（Documents / Together 拆 pane）
3. 更多優惠 tab
4. 刪 AiPlanner
5. QA、文件、部署
